apiVersion: v1
kind: Service
metadata:
  name: dpp-api
  labels:
    app: dpp-api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: dpp-api
